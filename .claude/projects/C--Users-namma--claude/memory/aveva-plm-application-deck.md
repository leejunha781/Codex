---
name: aveva-plm-application-deck
description: "User is building an English exec deck for an AVEVA Marine PLM SME role (target customer HD Hyundai), files in D:\\이력서"
metadata: 
  node_type: memory
  type: project
  originSessionId: 30ea7397-1aef-4814-afd9-b85cab286faf
---

The user is preparing an **English executive slide deck** to support a job application for **"AVEVA – Marine Principal Technical Support & Consultant – PLM SME, Busan"**. The target customer/account is **HD Hyundai Heavy Industries (HHI)**, shipbuilding/marine domain.

Working folder: `D:\이력서\AVEVA - Marine Principal Technical Support & Consultant – PLM SME, Busan\Proposal\`. The user iterates via versioned filenames (`_Corrected`, `_Rebuilt`, `_v2`, etc.) and often has several of these decks **open in PowerPoint at once** while working. (Original main deck was `Future_Industrial_PLM_Meeting_Deck_EN_Rebuilt.pptx`, 18 slides.)

**CURRENT CANONICAL EN deck (as of 2026-06-06): `Proposal\Future_Industrial_PLM_Meeting_Deck_EN.pptx` (default alias) and source version `Proposal\Future_Industrial_PLM_Meeting_Deck_EN_V5.pptx` — 37 slides, 27,704,970 bytes, saved 2026-06-06 15:48:08.** This supersedes `..._Hybrid_Safety_Final_v4.pptx`, `..._Rebuilt_v2.pptx`, and earlier variants. Use the default alias for "basic/default" references and V5 when preserving the explicit version name matters.

Theme/stack to stay consistent with: dark navy industrial look, white titles, translucent panels with cyan/orange accent bars, fonts **Aptos Display / Aptos**; the reference architecture is **FastAPI + PostgreSQL + OIDC (OAuth2) + E2E tests**, positioned as AVEVA-style Engineering-to-Operations PLM (digital thread, configuration/eBOM, change impact).

On 2026-06-03 I added slide 19 (Hardware⇄Software(API) architecture + an HD Hyundai step-by-step API client example) — built/QA'd in `..._Rebuilt_v2.pptx`, then merged into the ORIGINAL `..._Rebuilt.pptx` (now 19 slides) via `Slides.InsertFromFile`. Conventions the user chose for that slide (reuse for consistency): infra = on-prem HA, multi-yard, 1,000+ users; API base `https://plm.hhi.co.kr/api/v1`; generic PLM endpoint vocabulary `/objects`, `/structures`, `/ecr/{id}/impact`, `/approvals` (+ `/oauth/token`). Build/merge scripts live in `C:\Users\namma\.claude\plm_slide_work\`.

Full-deck review pass (2026-06-03, on v2, now 19 slides; user had moved the architecture slide to position 9): content/API-alignment/WBS/KPI all check out. Changes made: slide 10 Phase-1 list relabeled B-E → A-D (was missing "A"); slide 13 "Delivery WBS" text bars → a proportional Gantt timeline (20 wks, G1-G5 KPI gate diamonds + WBS→KPI mapping line); slide 14 "KPI model" text bars → a 4×2 scorecard (gauge bars + PASS/CONDITIONAL/FAIL card). Open optional items not yet done: slide 9 example endpoints are generic (/objects,/structures,/ecr,/approvals) vs the real reference API on slide 8 (kpi, readiness, coverage, quality_issues, configuration, evaluate/promote) — harmonize only if user wants.

Later (2026-06-03): added a new slide 20 to v2 only — an AI-gated Engineering→Manufacturing flow inside a "PLM app window" (E3D design → E-BOM+submit → approval-in-PLM → Effectivity[Hull/Block/Option/Date] → Baseline[frozen] → ECR→ECO → M-BOM+MCO → Impact+Approval), serpentine 2×4 cards with a magenta "AI" hexagon gate on every transition (PASS→promote / FAIL→flag) and live PLM status chips. v2 now 20 slides; the 3 review upgrades were mirrored into the ORIGINAL but this new slide 20 was NOT yet added to the original (offer pending). The two images the user pasted came through blank/white.

Later (2026-06-03): user moved that flow slide to position 12 and asked to AVEVA-accuracy-check it + add a 2D-drawing screen example. Regenerated slide 12 as a 9-step AVEVA-accurate flow (added "2D Production Drawing -> AutoCAD DWG/DXF" via AVEVA Draw; "E-BOM" -> "E-BOM/MTO"; per-step tool tags: AVEVA E3D/Marine, AVEVA Draw, AVEVA MTO for design vs PLM·API / +ERM for governance/mfg; AI hex gate on every transition). Inserted NEW slide 13: AVEVA Draw screen mockup (model tree, drawing sheet with PLAN/ELEVATION/ISO views + title block, "Export -> AutoCAD DWG/DXF" button) feeding a right-side "register in PLM" flow (DWG+MTO -> AI standard-check -> POST /api/v1/drawings 201 -> status chips). v2 now 21 slides. AVEVA tool mapping used: E3D/Marine=3D design, AVEVA Draw=2D drawings (DWG/DXF), MTO=material take-off/BOM, ERM=mfg; ECR/ECO/MOC/baseline/effectivity are the reference-PLM-layer concepts built over AVEVA. These slides are in v2 ONLY (not yet in original).

Later (2026-06-03): added slide 14 (after the drawing-screen slide) — a PLM-UI example: Requirement entered in the API-PLM UI (REQ-014 ballast water example) -> AI builds a VCRM (Verification Cross-Reference Matrix, 대분류) with 소분류 rows each holding a deliverable + verification status, and a Checklist (대분류) with 소분류 rows tagged Caution(개발 유의사항)/Reflect(요구사항 반영 확인) each with a deliverable; bottom approval gate: VCRM + Checklist + "all requirements reflected" must be approved -> PRODUCTION RELEASED (AI holds the gate otherwise). v2 now 22 slides. v2-only. Backups of all live decks are in Proposal\_backup_20260603_194545\.

Later (2026-06-03): per user — (a) moved the Requirement/VCRM slide to BEFORE the design flow (now pos 12, flow=13, drawing=14) so Requirement precedes Design; (b) regenerated VCRM slide to add an ADMIN add/delete example (toolbars "+ Add item / × Delete" + per-row × + API POST/DELETE /api/v1/{vcrm|checklist}/items) and reframed its approval gate to "APPROVED IN THE 'IMPACT + APPROVAL' STEP" (VCRM & Checklist approved together there); (c) edited the flow slide's step-9 card to "Affected objects + VCRM & Checklist approval"; (d) added NEW slide 15 "User-configurable PLM process — reshape the whole flow via API" (Process Designer admin UI with reorder/add/remove/gate controls + API GET/PUT/POST/DELETE /api/v1/process/{definition,steps,gates} + worked example = move Requirement before Design via PUT .../order). v2 now 23 slides. Cluster order: 12 Requirement/VCRM, 13 AI-gated flow, 14 2D drawing screen, 15 process-config. All these example slides are v2-ONLY (original not updated). Note: deck's manual page-number text shapes are broadly out of sync after all the reordering (pre-existing); only my new slides' numbers were set to their positions.

Later (2026-06-03, v2, 23 slides): (1) HD Hyundai alignment — chose Korean-shipbuilding bilingual. Flow slide (13) regenerated fully bilingual: 상세설계/생산도면/자재산출/결재·승인/유효성(호선·블록)/형상기준선/설계변경/생산BOM/영향분석·승인, + a 권한(RBAC) band (설계자 Engineer·검토자 Checker·승인자 Approver·관리자 Admin, OIDC scopes). Slides 12/14/15 got bilingual titles+headers via targeted EditByPrefix (요구사항/검증요구사항 매트릭스/체크리스트/2D 생산도면/관리자 권한). Korean renders fine via COM (UTF-8 .ps1 read OK). (2) Visibility redesigns: slide 4 -> Engineering↔Operations positioning map (AVEVA in sweet-spot); slide 19 -> convergence diagram (4 vendor strengths funnel into AVEVA-based PLM hub); slide 21 -> 3x3 Likelihood×Impact risk matrix + risk→mitigation legend; slide 23 (closer) -> dark scrim panel behind text for contrast over the photo. (3) v2 backup taken BEFORE edits. User consolidated all backups into Proposal\_backup_20260603_194545\ (incl. ..._v2_BACKUP_20260603_210257.pptx = pre-edit v2). Helper pattern that works: regen via DelAll+rebuild OR targeted EditByPrefix; always force-close orphan + open fresh; Retry wrapper around COM color sets; inline if-expression NOT allowed as a function arg (precompute to a var).

Later (2026-06-03): user kept editing v2 in parallel (it grew to 24 slides — a duplicate "Executive thesis" added at slide 4; some of my earlier bilingual edits reverted to EN). Two new deliverables: (1) HD현대중공업 제안서 양식 (proposal TEMPLATE) — new file Proposal\HD현대중공업_제안서_양식.pptx, 5 slides (표지/목차/본문양식×2/맺음), light/white theme matching HD's slide style: green chevron logo placeholder ("HD" green chip + 현대중공업), tagline "Unlocking the Limitless Potential of the Ocean", green section bars, footer page numbers. HD brand accent ≈ teal-green #00A887. NOTE: logo is a stylized placeholder — replace with official HD logo. (2) Full Korean edition of v2 → Proposal\Future_Industrial_PLM_Meeting_Deck_KO_Rebuilt_v2.pptx (24 slides). Method that WORKED for bulk translation: extract all shape text (CR/LF→¶ normalized) → author 3 UTF-8 map files build9_map*.txt with "EN¶norm|||KO" lines → apply script reads maps (Get-Content -Encoding UTF8), iterates all shapes/groups/tables, sets TextRange.Text by exact normalized-key match (367 hits). Kept English: endpoints, code, file names, product/vendor names, IDs, numbers, status codes (intentional). Korean renders via PowerPoint font fallback (no font change needed). The KO file got deleted once by user cleanup — just re-copy from v2 and re-run apply. Translation scripts/maps in C:\Users\namma\.claude\plm_slide_work\build9_*.

Later (2026-06-06): EN deck `Future_Industrial_PLM_Meeting_Deck_EN_Rebuilt_v2.pptx` (in Proposal\) is now **30 slides, 23 MB**, with **9 AI-generated marine/PLM concept images** embedded and placed one-per-slide on slides **1, 5, 7, 9, 13, 16, 23, 27, 30** (image1–image9; no orphans). Source images were AI-generated and downloaded as `생성된 이미지 N.png` in `C:\Users\namma\Downloads\` (image4=2631KB=생성된 이미지 1, image6=2149KB=생성된 이미지 2; rest pasted into chat only). Deck was saved 03:16:42 by the user (PowerPoint not running when checked). NOTE: images pasted directly into a Claude chat are NOT accessible to me as files on disk — to insert/save them I need the actual files in a folder, not a paste.

Later (2026-06-06): meeting-readiness pass on EN `..._Rebuilt_v2.pptx` for an overseas dev+planning meeting. Full 30-slide flow reviewed via COM text-dump + PNG render — judged meeting-ready (7-section agenda on slide 2 maps cleanly to the slides; page numbers sequential/correct; no genuinely weak/empty slides — deck is dense and complete). Added a 2-slide **Glossary appendix** (now 31 slides→ **32 slides**): slide 31 "Glossary 1/2 — PLM, Engineering & Marine terms" (2 cols, cyan PLM/config/change + amber marine/naval/class), slide 32 "Glossary 2/2 — Products, Architecture, Security & Infrastructure" (green products + cyan arch/security/infra), ~45 abbreviations total. Built via `Slides.AddSlide(idx, Slides.Item(8).CustomLayout)` then clear shapes + rebuild; header matched exactly to standard content slides — title **Aptos Display 21.5 bold white @ L34/T18**, subtitle **Aptos 9.6 #C7D6E5 (BGR 15062727)**, brand **Aptos 7.6 #93A8C0 (12626067) @ L800/T24 right-align**, divider rect @ T80, page-num 6.5 @ L900/T8. Palette BGR ints: navy bg **3678739**, cyan **15646772**, amber **3846888**, green **9224777**, white 16777215. Build script `plm_slide_work\build_glossary2.ps1`; pre-edit backup in `Proposal\_backup_20260606_meeting\`. PS5.1 gotchas hit: COM launch via Bash subshell fails 0x80080005 (use the PowerShell tool / interactive session, or `Start-Process POWERPNT /automation` to warm it, then `GetActiveObject`); concat expressions (`"a"+[char]0x2014+"b"`) can't be inline function-call args — precompute to vars; use `-split '\|\|\|'` not `.Split("|||")` (overload-ambiguous in .NET Framework). Then (same day, user approved) did the contrast + pointer pass via `plm_slide_work\edit_scrim.ps1`: slides **5 & 7 left untouched** — checked geometry/fills and the photo sits *beside* the text (text is on navy or in opaque dark cards, not over the photo), so no scrim needed. Slide **16**: deepened the EXISTING scrim over the "VISUAL PROOF" photo (shape "Rectangle 3", Fill.Transparency 0.28→0.15). Slide **30**: deepened the EXISTING full-bleed bg scrim (shape "Rounded Rectangle 2", a solid near-black at Fill.Transparency 0.6→0.30) so title/subtitle/"WHAT APPROVAL UNLOCKS" gain contrast; content cards there are already ~0.05 transp (opaque). Slide **2**: added a cyan right-aligned callout "Abbreviations → Glossary appendix, p.31–32" (new shape "GlossaryPointer" @ L540/T55, Aptos 9, color 15646772) — additive, existing shapes untouched. Lesson: the deck's photo slides put text BESIDE the photo on navy or in near-opaque cards; only the genuinely text-over-photo regions (16 bottom-left, 30 full-bleed) needed scrim deepening — verify geometry before adding scrims. Cannot generate raster images in this session (no image-gen tool) — substantive visuals are all shape diagrams anyway.

Later (2026-06-06): user supplied 2 NEW purpose-built concept images (16:9, 1672x941, dark-navy theme matching deck) and asked to place them. The user's curated image set lives in `Proposal\plm_integrated_english_package\04_Presentation Image\` (01–08 + 16; 01–07 already embedded on slides 1/5/7/9/13/23/27/30). Placed via `plm_slide_work\edit_images.ps1`: **`08_open_aveva_control_plane_generated.png` (=Downloads\생성된 이미지 1, 2631KB) → slide 8 BACKGROUND** (`$slide.Background.Fill.UserPicture(path)` after `FollowMasterBackground=$false`, + a full-bleed dark scrim rect at SendToBack/transp 0.35) — slide 8's content panels are all opaque (transp 0) so the federation-hub scene shows only in gaps/margins/header = cohesive, not cluttered. **`16_continuous_naval_assurance_loop_generated.png` (=생성된 이미지 2, 2149KB) → slide 16**: deleted the generic "Picture 2" in the VISUAL-PROOF panel and `AddPicture`'d img16 at the same rect (38,236,520,293), `ZOrder(1)`, and lightened that panel's scrim "Rectangle 3" transp 0.15→0.40 so the numbered loop shows under the title/caption. No new slides → still **32 slides**, no renumbering. Path lessons: (1) the Read tool and typed tool-commands choke on the folder's en-dash "–" (U+2013) + Korean — copy images to an ASCII path (`plm_slide_work\imgNN.png`) via a Write-tool .ps1 using the exact literal path, then operate on the copy; (2) embed pictures with SaveWithDocument=msoTrue so the source path is irrelevant after save; (3) those generated images were found one level deep (`...\04_Presentation Image\`), not directly in Proposal — earlier listing used `-Recurse`. Backup of pre-image deck still in `Proposal\_backup_20260606_meeting\` (PREEDIT) — note that predates the image edits; re-copy before further edits if a fresh restore point is wanted.

Later (2026-06-06): full structural/editability review requested on EN v2. Preserved source and created `Proposal\Future_Industrial_PLM_Meeting_Deck_EN_Rebuilt_v2_VisualReviewed.pptx`; pre-edit backup is `Proposal\_backup_20260606_052316\Future_Industrial_PLM_Meeting_Deck_EN_Rebuilt_v2_BACKUP_20260606_052316.pptx`. Current deck is **33 slides**. Verified Glossary slides 31–33 already use **6 native PowerPoint tables** total (two tables per slide); terminology and definitions are written directly in table cells, not standalone text boxes. Ran a safe structure pass on representative UI/process slides: merged **60 exact 1:1 empty-shape + overlaid-textbox pairs** into native shape text (cards, step circles, process descriptions, footers) while leaving multi-textbox cards untouched to preserve mixed formatting. Key QA renders stayed visually identical/readable; all 33 slides rendered successfully, PPTX is unlocked, and the existing explanatory images/backgrounds were retained because the full-deck contact sheet showed adequate visual coverage without adding clutter. Scripts/QA: `plm_slide_work\audit_visualreview.ps1`, `analyze_shape_text_candidates.ps1`, `merge_shape_text_safe.ps1`, `verify_visualreview.ps1`; renders/report under `plm_slide_work\visualreview_current\`.

Later (2026-06-06): meeting-readiness review of the active working file `Proposal\Future_Industrial_PLM_Meeting_Deck_EN_Rebuilt_v2.pptx` (not VisualReviewed). Full 33-slide render/contact sheet confirmed a coherent meeting flow: current state (3–5) → public signals (6) → win-above ideas (7–12) → proposal/delivery (13–25) → future-state compare (26) → review/risks/evidence (27–29) → decision (30) → glossary appendix (31–33). No weak/empty content slide and no additional image needed; slide 18 already contains reference-image-based raster icons/background and is visually explanatory. Meeting-navigation improvement applied to slide 2: added 7 color-matched page-range chips (`p.3–5`, `p.6`, `p.7–12`, `p.13–25`, `p.26`, `p.27–29`, `p.30`) and corrected Glossary pointer from `p.31–32` to `p.31–33`. No content removed. Pre-edit backups: `Proposal\_backup_20260606_062739\...MEETING_REVIEW_BACKUP...pptx` and before navigation `Proposal\_backup_20260606_063237\...BEFORE_MEETING_NAV...pptx`. Final verification: 33 slides, 6 native tables, 16 picture shapes, 7 navigation chips, unlocked.

Then (same day, user approved "add the candidates too") placed 3 more via `plm_slide_work\edit_images2.ps1`: **slide 9** — swapped existing "Picture 5" (small convergence img) → **img06** (`06_a_highly_detailed_clean_futuristic_infographic` = brain/AI over converging specialist domains) in the right-panel rect (558,116,366,206), kept the panel's existing scrim "Rectangle 6" (transp 0.36) so the "NEW AVEVA-BASED PLM / Five specialist strengths → one decision control plane" caption stays readable; **slide 13** — swapped "Picture 13" → **img05** (`05_..isometric_3d` = clean 4-layer stacked platform) in the left rect (10,97,480,338), pairs with the 5 named layer texts on the right; **slide 21** — **img16** (the loop, reused) as slide BACKGROUND + dark scrim (transp 0.30) behind the opaque 6-step cards (atmospheric). Rule that emerged: when a dense slide ALREADY has a small relevant image, SWAP it for the better purpose-built one (slides 9/13/16) rather than a full-bg (mostly hidden behind opaque panels); use full-bg only where there's no existing image and panels are opaque (slides 8/21). All swaps: record L/T/W/H, Delete, AddPicture(path,0,-1,L,T,W,H), ZOrder(1). Net image map now: slide 8 bg=img08, 9=img06, 13=img05, 16=img16(VISUAL-PROOF panel), 21 bg=img16. Still 32 slides, 22.5 MB, no renumbering. Curated source set: `Proposal\plm_integrated_english_package\04_Presentation Image\` (01–08,16, all 1672x941 16:9).

Later (2026-06-06): **hybrid-architecture correction** on EN `..._Rebuilt_v2.pptx` (33 slides). User clarified the platform must be a HYBRID, not Linux-only: **AVEVA E3D/Marine/Draw authoring runs on Windows** (supported design environment / GPU workstations / Citrix-published), and **Linux hosts the Open PLM Control Plane only** (API · SQL · AI Gate · Evidence · Integration Gateway) + Cloud/CONNECT; E3D connects via **Local Agent / Adapter** (FastAPI issues commands, the E3D/Hull Agent or Plugin executes and returns a callback — it is NOT called directly, and E3D is NOT embedded in/hosted on the Linux servers). Main fix = **slide 15** ("Hardware ↔ Software (API) Architecture", the hardware-config slide): retitled "Hybrid: Windows Authoring ↔ Linux PLM Control Plane (API)"; section 2 relabeled "AVEVA PLM SOFTWARE" → "OPEN PLM CONTROL PLANE (Linux)"; the **"CAD / AVEVA E3D" box was replaced** by two control-plane boxes "AI Gate / Evidence" + "Integration Gateway" (other 4 boxes kept: Objects&Vault, Structures/eBOM, ECR&Impact, Approvals/Workflow); infra section 3 retitled "Linux cluster + Windows authoring tier", App-server label → "App · API · AI Gate (Linux)", and the wide infra note (TextBox 47) rewritten to lead with the Windows authoring tier (GPU workstations, Local Agent/Adapter, "not hosted on the cluster") + fixed "GPU node for 3D/digital-twin" → "GPU node for AI gate / digital-twin"; bottom caption + the two inter-panel connectors (TextBox 15/27) reworded to the command→callback / adapter model. Lighter one-line clarifiers also applied: **slide 13** Integration Layer → "AVEVA E3D/Marine (Windows authoring)…"; **slide 16** step-1 "POST /events" → "Agent → POST /events"; **slide 20** card-4 body → "…on the Linux control-plane servers; AVEVA E3D authoring runs on Windows clients…"; **slide 21** event source → "E3D/Marine (Windows) + PLM". The HD Hyundai API example (slide 15 right column, oauth/objects/structures/ecr/approvals) was already generic and left unchanged. QA: re-rendered slides 13/15/16/20/21 to PNG; first pass overflowed (my replacement text was longer than originals → title/subtitle/headers/caption wrapped & overlapped) so a **fit-up pass shortened wording and set TextBox 3=12pt, TextBox 47=7.5pt + height 42** — second render clean, no clipping. Pre-edit backup: `Proposal\_backup_20260606_hybridarch\..._BEFORE_HYBRID.pptx`. Scripts/renders in `plm_slide_work\` (`dump_v2.ps1`, `geom15.ps1`, `edit_hybrid.ps1` + `edit_hybrid2.ps1` fit-up, `render_hybrid.ps1`, `verify_hybrid.ps1`; PNGs in `plm_slide_work\hybrid_render\`). Method note: the deck's "Text N" two-line shapes store the line break as a paragraph — edit `TextRange.Paragraphs(2).Text` to keep the bold label (used on slide 13 Text 8); for the dense single-style boxes, setting whole `TextRange.Text` is fine. Ran fine via the PowerShell tool (pwsh7, UTF-8) — `Get-Content -Raw | Invoke-Expression`; arrows/middle-dot/em-dash built from `[char]` codes render correctly.

Later (2026-06-06, pass 3): user asked to "re-revise the presentation to match the changes". Ran a **full-deck keyword audit** (scan_terms.ps1 → scan_terms.txt: linux/windows/e3d/marine/host/embed/fastapi/control plane/adapter/agent across all 33 slides) — confirmed the rest of the deck speaks in conceptual "AVEVA control plane / AVEVA-based PLM" terms that are already compatible with the hybrid model; **no remaining E3D-on-Linux or FastAPI-calls-E3D-directly statements**. Added two reinforcing one-liners so the narrative (not just the hardware slide) carries the split: **slide 13** concept subtitle (Text 1) now ends "— Windows authoring + Linux control plane + Cloud/CONNECT"; **slide 18** subtitle (TextBox 96) → "Design in AVEVA (E3D/Marine + Draw, Windows authoring) → governed & tracked in the Linux API-built PLM (control plane), AI-gated at every step". Skipped the glossary E3D/Marine cell (its "data-centric 3D design" definition isn't OS-specific, so not wrong). Backup before this pass: `_backup_20260606_hybridarch\..._BEFORE_PASS3.pptx`. **Stale-window gotcha (important):** the user reported "changes not applied" — but a direct on-disk zip read (check_disk.ps1: copy file → ZipFile.OpenRead → search ppt/slides/slide15.xml) proved the save HAD persisted; the open PowerPoint window was just showing a cached pre-edit copy. Fix without data loss = close the open copy WITHOUT saving (set `$pres.Saved=$true` then `$pres.Close()`, never Save — saving the stale copy would overwrite disk), then reopen from disk and verify via the LIVE shape text (reload_final.ps1). Warn the user not to Ctrl+S the stale window. Also: GetActiveObject doesn't reliably re-find an already-open copy between separate script runs (sometimes opens fresh + closes), so always end an edit session with an explicit close-without-save + reopen to guarantee the visible window matches disk.

Later (2026-06-06, learned v4): the user evolved my hybrid-edited v2 into a new canonical EN deck **`Proposal\Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4.pptx`** — **34 slides**, now **27.7 MB** (was 25.6 MB at 12:52; grew to 27,689,354 bytes by 13:52 — likely additional images added directly in PowerPoint), saved 13:52. This is now the **latest/primary EN deck** (supersedes `..._Rebuilt_v2.pptx` for the architecture topic). What's new vs v2: (1) a **NEW dedicated correction slide, now slide 16 "Final hybrid deployment correction — authoring vs control plane"** (insert pushed old 16→17 … 33→34), containing 3 zone cards [**Windows Authoring Zone** = E3D/Marine/Hull Design, AVEVA Draw, MTO, Local Agent/Plugin/PML.NET Bridge, "Windows designer PCs or supported AVEVA environments", "No direct Linux hosting assumption"] · [**Linux Open PLM Control Plane** = FastAPI/OpenAPI, PostgreSQL, Evidence Store, AI Gate + Rule Guardrails, Integration Gateway + Callback Receiver, Keycloak/OIDC/RBAC] · [**Cloud/CONNECT + Adapters** = CONNECT, AIM, PI Web API, AVEVA Edge, NAPA/Class/CFD/FEM adapters, ERP/MES, Document Vault], PLUS the user's original 4-row correction table ("AVEVA Marine/E3D on Linux" → "E3D/Marine/Hull runs in supported Windows authoring env; Linux runs API/SQL/AI Gate/Evidence/Integration", etc.) and an **Interview line**: "E3D/Marine authoring stays Windows-centered; Linux owns FastAPI, PostgreSQL, AI Gate, Evidence Pack, Integration Gateway and Callback Receiver." (2) **Standardized hybrid vocabulary** the user wants used consistently going forward: **Local Agent / Plugin / PML.NET Bridge** as the Windows↔Linux connector; **FastAPI issues REST commands → Local Agent/Plugin executes E3D/Hull work → JSON callbacks (Callback Receiver)**; E3D/**Marine/Hull** authoring; "supported AVEVA environments"; "no direct Linux hosting assumption". (3) These were propagated across the deck — slide 13 Integration Layer ("…via Local Agent/PML.NET Bridge"), slide 15 connectors/notes, slide 17 step1 "Local Agent → POST /events", slide 19 "(E3D/Marine + Draw on Windows) → Local Agent/Plugin callbacks → Linux API-built PLM", slide 22 "Windows Local Agent/Plugin emits…/ Windows E3D/Marine + Local Agent", slide 30 deployment-boundary note, slide 31 "proves Windows Authoring + Linux Control Plane / E3D/Hull Local Agent pilot slice", and the **glossary (now slides 32/33/34)** where AVEVA E3D/Marine def became "Windows-centered 3D ship/plant authoring; connect via Agent/Plugin/API bridge". Net: v4 is internally consistent with the hybrid model and is the version to edit/learn from next; reuse the Local Agent/Plugin/PML.NET Bridge + Callback Receiver wording. Dump of full v4 text saved at `plm_slide_work\dump_v4.txt` (via `dump_v4.ps1`).

Later (2026-06-06, Codex→Claude handoff): copied Codex-only NAPA work folders into Claude work storage: `C:\Users\namma\.claude\plm_slide_work\napa_review\` (8 rendered PNGs) and `C:\Users\namma\.claude\plm_slide_work\napa_assurance_20260606\` (39 files: build/polish/finalize scripts plus review/final_all renders). In the Claude copy, the three scripts' review paths were changed from `.Codex` to `.claude`; source Codex files were left unchanged. These are historical NAPA Assurance slide/reference artifacts around `Future_Industrial_PLM_Meeting_Deck_EN_Rebuilt_v2_NAPA_Strategy_FinalLayoutFixed_v4.pptx`, while the latest primary EN architecture deck remains `Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4.pptx`.

Later (2026-06-06, confirmed v4 from disk): re-opened **`Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4.pptx`** by PowerPoint COM and confirmed it is **34 slides**, **25,638,360 bytes**, saved **2026-06-06 12:52:43**. Fresh dump saved at `C:\Users\namma\.claude\plm_slide_work\dump_v4_confirm.txt`. Verified exact markers exist in the file: slide 16 has "Final hybrid deployment correction", "Windows Authoring Zone", "Linux Open PLM Control Plane", "Cloud / CONNECT + Adapters", "PML.NET Bridge", "Callback Receiver", "No direct Linux hosting assumption", and the interview line; slide 15 uses "Hybrid: Windows Authoring + Linux Control Plane + Cloud/CONNECT"; slide 19 uses "E3D/Marine + Draw on Windows → Local Agent/Plugin callbacks → ... Linux API-built PLM"; glossary slide 33 defines AVEVA E3D/Marine as "Windows-centered 3D ship/plant authoring; connect via Agent/Plugin/API bridge." Treat this v4 file as the canonical English architecture deck unless the user names a newer file.

Later (2026-06-06, v4 visual support): generated a new 16:9 dark-navy hybrid deployment background via built-in imagegen, referencing the user's existing marine/PLM images. Saved project copy as `Proposal\plm_integrated_english_package\04_Presentation Image\17_hybrid_deployment_boundary_generated.png` and ASCII working copy as `C:\Users\namma\.claude\plm_slide_work\img17_hybrid_boundary.png`. Applied it only to **slide 16** of `Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4.pptx` as the slide background, with a dark full-slide scrim shape named `GeneratedHybridBgScrim` behind all editable content; strengthened top zone cards slightly (Fill.Transparency 0.05). Rationale: slide 16 was the main text-heavy hybrid correction slide and benefited from a visual deployment boundary; slide 15 was left unchanged because its API/hardware/code content is already dense and adding a background would reduce readability. Backup before edit: `Proposal\_backup_20260606_v4_visualsupport\Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4_BEFORE_VISUALSUPPORT_*.pptx`. QA render: `C:\Users\namma\.claude\plm_slide_work\v4_visualsupport_render\slide16_visualsupport.png`; verified no overlap/clipping and PowerPoint was reopened at slide 16 after save. PPTX saved 2026-06-06 13:16:24, size 27,688,693 bytes.

Later (2026-06-06, v4 slide 16 visual-panel rework): user asked for the slide-16 image to appear as an inserted image, not a background, and for original content to be resized like other image-bearing slides. Reworked **slide 16**: reset slide background to native dark navy, removed `GeneratedHybridBgScrim`, inserted `17_hybrid_deployment_boundary_generated.png` as normal picture shape **`HybridBoundaryVisual` @ L40/T104/W400/H225** with cyan border, added small caption `HybridBoundaryCaption`, compressed the original three zone cards to a right-side vertical stack, and resized the correction table + interview line into the lower band. Deleted the old horizontal-card arrows (`Right Arrow 6`, `Right Arrow 7`) because they overlapped after the new vertical-card layout. QA render saved at `C:\Users\namma\.claude\plm_slide_work\v4_slide16_visualpanel_render\slide16_visual_panel_final.png`; verified `HybridBoundaryVisual` exists as a normal picture, `GeneratedHybridBgScrim=False`, 34 slides, read/write. Fresh backup before this rework: `Proposal\_backup_20260606_v4_slide16_visualpanel\Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4_BEFORE_SLIDE16_VISUALPANEL_*.pptx`. PPTX saved 2026-06-06 13:28:21, size 27,688,643 bytes.

Later (2026-06-06, v4 slide 15 infra-card fix): user pointed at the slide-15 Infrastructure panel and asked to add a visible Windows authoring shape with explanation while preserving other content. On **slide 15**, kept the existing API layer, Open PLM Control Plane, right-side API example, and the four Linux infra cards. Converted the previous free-text Windows authoring note into a new rounded rectangle card **`WindowsAuthoringTierCard` @ L36/T464/W476/H48** with icon **`WindowsAuthoringTierIcon`**, text: "Windows Authoring Tier / GPU-Citrix workstations: AVEVA E3D · Marine · Hull · Draw / Local Agent / Plugin / PML.NET Bridge → Linux Control Plane; not hosted on Linux." Slightly reduced the four Linux infra card heights to make room; no other visible content removed. Backup before edit: `Proposal\_backup_20260606_v4_slide15_windows_card\Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4_BEFORE_SLIDE15_WINDOWS_CARD_*.pptx`. QA render: `C:\Users\namma\.claude\plm_slide_work\v4_slide15_windows_card_render\slide15_windows_authoring_card_final.png`; PowerPoint reopened at slide 15.

Later (2026-06-06, v4 AI capabilities reinforcement): user provided detailed content on two AVEVA AI tools and asked to verify + reinforce the v4 deck. Confirmed neither tool was mentioned by name in the existing 34-slide deck (only generic "physics-guarded AI" / "AI gate" / "CONNECT industrial intelligence" appeared). Applied **5 targeted changes** to `Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4.pptx`, which is now **35 slides**:
1. **Slide 6** (AVEVA public signals): TextBox 20 updated to include "Industrial AI Assistant (LLM)" and "Generative / Predictive Design AI" in the AVEVA capability list; TextBox 21 opportunity text now reads "secure AI Q&A on ship data, generative design automation and design-to-operations feedback."
2. **Slide 9** (AVEVA differentiation): TextBox 56 bottom item updated to "Industrial AI Assistant (CONNECT LLM) + Generative / Predictive Design AI + federated solvers + physics-guarded AI + evidence + operations learning".
3. **Slide 19** (AI-gated flow note): TextBox 185 updated to add "Generative Design AI assists piping routing + clash detection" at the design step.
4. **Slide 33** (Glossary 2): Two new rows inserted **before Teamcenter (row 7)** in the AVEVA products table using `Table.Rows.Add(tcRow)`: "AVEVA Industrial AI Asst." → Industrial AI Assistant (AVEVA CONNECT) → "Secure industrial LLM on CONNECT; drawing Q&A, asset specs, change history, onboarding; no data leaves CONNECT; role-gated access." and "AVEVA Generative Design AI" → Generative / Predictive Design AI (Unified Engineering) → "AI-suggested piping routing, clash resolution, design simulation; requires human validation against class / marine standards."
5. **NEW Slide 35**: "AVEVA AI Capabilities — Industrial AI Assistant + Generative Design AI" appendix slide created from scratch (`Slides.Add(35, 12)` ppLayoutBlank). Two-column layout (left = AI Assistant, right = Generative Design AI) each with WHAT IT IS / USE CASES / ADVANTAGE / CONSTRAINT sections. Section heading labels (WHAT IT IS / USE CASES / ADVANTAGE / CONSTRAINT) styled teal (0x33CF99) bold via paragraph-level `Font.Color.RGB`. All text white; title 18pt bold; section column labels 9pt bold teal; content 9pt white; footer "Future Industrial PLM / Appendix / 35" at bottom. Backup before edits: `Proposal\_backup_20260606_aitools\v4_BEFORE_AITOOLS.pptx`. QA renders in `plm_slide_work\ai_render\` (slides 6,9,19,33,35). Scripts: `plm_slide_work\edit_ai_tools_v4.ps1` (edits+render) + `plm_slide_work\fix_slide35_color.ps1` (text color fix). PPTX saved 2026-06-06 14:40:56, size ~27,692,233 bytes.

**Two AVEVA AI tools — standardized description for all future sessions:**
- **AVEVA Industrial AI Assistant (AVEVA CONNECT)**: Industry-specific LLM on the CONNECT cloud platform. Use: drawing review Q&A, asset spec lookup, change history tracking, onboarding. Pro: confidential data stays inside CONNECT (no external exposure), role-gated access. Con: initial data connection/indexing required; scope limited by user permissions.
- **AVEVA Generative / Predictive Design AI (Unified Engineering)**: Design automation and predictive AI in AVEVA Unified Engineering. Use: piping layout optimisation, 3D clash detection/resolution, AI-suggested routing, simulation acceleration. Pro: automated optimised pipe routing in complex ship spaces, reduces lead time. Con: outputs must be manually validated by marine engineer against classification/shipbuilding standards before release.

Later (2026-06-06, v4 lightweight AI slide): user asked for lightweight alternatives to the heavy AVEVA AI tools and wanted them added to the presentation. Added **slide 36** "Lightweight AI Alternative — Rule-Based Gate → On-Prem RAG → AVEVA CONNECT" to `Future_Industrial_PLM_Meeting_Deck_EN_Hybrid_Safety_Final_v4.pptx` (now **36 slides**, ~27,695,844 bytes). Also updated **slide 9 TextBox 52** to append "CONNECT (AI Asst.) + Lightweight RAG (pgvector + Ollama)" to the AVEVA differentiation item list. Backup: `Proposal\_backup_20260606_lightweight_ai\v4_BEFORE_LIGHTWEIGHT_AI.pptx`. Script: `plm_slide_work\add_lightweight_ai_slide36.ps1`. QA renders in `plm_slide_work\ai_render\slide9_lw.png`, `slide36_lw.png`.

**Slide 36 structure (2-column, dark navy, white text, teal/amber headers):**
- Left "LIGHTWEIGHT STACK (Tier 0 + Tier 1)":
  - TIER 0 — RULE-BASED AI GATE (zero license, immediate): FastAPI+Pydantic validation rules for drawing standards, PLM metadata, VCRM coverage; deterministic <10ms, no GPU
  - TIER 1 — ON-PREM RAG (pgvector + Ollama, existing Linux nodes): pgvector = one `CREATE EXTENSION vector` on existing PostgreSQL; embed ship drawings/specs/asset data/change history as vectors; Ollama = local LLM (Llama 3.2 3B/8B, Phi-3 Mini, Mistral 7B), OpenAI-compatible API, CPU-only viable for 7B, no data leaves shipyard; drop-in `/ai/query` + `/ai/embed` FastAPI endpoints
  - LIGHTWEIGHT PIPING OPTIMISATION: Python scipy+networkx constraint solver with naval engineering rules + 3D voxel clash grid (no ML model, no Generative Design AI license)
- Right "WHY LIGHTWEIGHT FIRST + PHASED UPGRADE PATH":
  - Why: CONNECT AI needs cloud sub + data indexing + GDPR review; Generative Design AI needs Unified Engineering license + GPU workstations; Tier 0-1 runs on existing 4-node Linux HA cluster + PostgreSQL
  - Phased path: Tier 0 (now, no license, no GPU, rules) → Tier 1 (4-8 wks, OSS free, CPU/1 GPU, on-prem RAG) → Tier 2 (scale, AVEVA subscription, cloud GPU, CONNECT AI) → Tier 3 (full, Unified Engineering, workstation GPU, Generative Design AI)
- Footer: "Tier 0-1 OSS stack: FastAPI (MIT) | PostgreSQL | pgvector (MIT) | Ollama (MIT) — all run on the existing Linux HA cluster and PostgreSQL already in the reference architecture."

Later (2026-06-06, v4 AI slide graphics + reorganization): added column-card visuals to slides 35-36 and repositioned them from the appendix into the main meeting flow. Deck is now **36 slides**, ~27,697,151 bytes, saved 2026-06-06 15:15. Backup before this pass: `Proposal\_backup_20260606_reorganize\v4_BEFORE_REORGANIZE.pptx`. Script: `plm_slide_work\reorganize_deck_v4.ps1`. QA renders in `plm_slide_work\ai_render\slide*_v5.png`.

**Visual enhancements (slides 13-14):** Each slide now has four new shapes: left column rounded-rect card (`AICardLeft`/`LWCardLeft`, dark navy fill + teal border, ZOrder to back), right column rounded-rect card (`AICardRight`/`LWCardRight`, same), thin teal accent bar under subtitle (`AIAccentBar`/`LWAccentBar`), and thin teal vertical divider line between columns (`AIDivider`/`LWDivider`).

**Repositioning:** Old slides 35 → new slide 13 ("AVEVA AI Capabilities"), old slide 36 → new slide 14 ("Lightweight AI Alternative"). `Slides.Item(35).MoveTo(13)` then `Slides.Item(36).MoveTo(14)` — after first MoveTo, old 36 stays at index 36 (it was after the moved slide); second MoveTo correctly inserts it at 14. Old slides 13-34 shift to positions 15-36.

**New deck slide order:**
- p.1-2: Cover / Agenda
- p.3-5: Current State (competitor comparison, PLM landscape, Phase 1 baseline)
- p.6: Public Signals
- p.7-12: Win-Above (thesis, gap, NAPA, Siemens, Dassault, absorb-and-move-beyond)
- **p.13: AVEVA AI Capabilities** (Industrial AI Assistant + Generative Design AI — WHAT IT IS / USE CASES / ADVANTAGE / CONSTRAINT for each)
- **p.14: Lightweight AI Alternative** (Tier 0-3 model: rule-based gate → on-prem RAG → AVEVA CONNECT → Generative Design AI)
- p.15-27: Proposal + Delivery (Target Architecture, Executable Ref, Hybrid HW, Hybrid Correction, Pilot, VCRM, AI-gated Flow, 2D Drawing, Version Control, Naval Assurance, Process Config, Gate Flow, WBS)
- p.28: KPI gates
- p.29-31: Review (Future-state compare, Review board, Risks)
- p.32: Close (Evidence base)
- p.33: Decision
- p.34-36: Glossary 1/3, 2/3, 3/3

**Page numbers updated:** All 24 slides updated (no misses). New slides 13-14 page numbers changed from "35"/"36" to "13"/"14" (shapes AIPgNum/LWPgNum). Old slides 13-34 (now 15-36) page numbers all incremented by 2 — found via exact text-match of old page number string on each slide; shape names varied per slide.

**Section labels:** Footer "Appendix" chip on slides 13-14 changed to "AI STRATEGY" (shapes AIAppendix/LWAppendix).

**Slide 2 navigation updated:**
- MeetingNavRange3 (WINNING IDEAS): p.7–12 → **p.7–14** (AI strategy slides added to winning-ideas section)
- MeetingNavRange4 (PROPOSAL + DELIVERY): p.13–25 → **p.15–27**
- MeetingNavRange5 (FUTURE-STATE COMPARE): p.26 → **p.28**
- MeetingNavRange6 (REVIEW): p.27–29 → **p.29–31**
- MeetingNavRange7 (CLOSE): p.30 → **p.32**
- GlossaryPointer: p.31–33 → **p.34–36** (also corrects a pre-existing 1-page offset in the pointer)

Later (2026-06-06, default save): user asked "기본으로 저장" for `Future_Industrial_PLM_Meeting_Deck_EN_V5.pptx`. Created the same-folder default copy `Proposal\Future_Industrial_PLM_Meeting_Deck_EN.pptx` from V5; both files are 27,704,970 bytes with LastWriteTime 2026-06-06 15:48:08. COM-opened V5 read-only and verified it has 37 slides. Treat `Future_Industrial_PLM_Meeting_Deck_EN.pptx` as the default working deck and `Future_Industrial_PLM_Meeting_Deck_EN_V5.pptx` as the explicit versioned source.

Later (2026-06-06, merge v4 visuals into V5): user asked to merge images from `Future_Industrial_PLM_Meeting_Deck_EN__v4.pptx` pages 13-15 into the default presentation file `Future_Industrial_PLM_Meeting_Deck_EN_V5.pptx`, while preserving V5 page 13-15 content and adjusting shape sizes. Backed up V5 to `Proposal\_backup_20260606_185129_merge_v4_images_13_15\Future_Industrial_PLM_Meeting_Deck_EN_V5_BEFORE_MERGE_V4_IMAGES_13_15.pptx`. Copied each v4 slide's `Picture 8` into V5 slides 13-15 as `MergedV4Image13/14/15` with frame shapes, then adjusted layout: slide 13 image as small visual reference inside the Design AI card; slide 14 image as a small non-overlapping thumbnail in the approval rationale card; slide 15 image as the main left visual inside the voxel-routing concept card. Removed temporary captions after QA to avoid text collisions. Exported V5 to PDF and rendered all 37 pages successfully; final QA render folder: `C:\Users\namma\.claude\plm_slide_work\merge_v4_images_final3\`. Final V5 size 32,910,310 bytes, saved 2026-06-06 18:55:53.

Later (2026-06-06, default save switched to V6): user requested "save the default presentation file" for `Proposal\Future_Industrial_PLM_Meeting_Deck_EN_V6.pptx`. Copied V6 to the unversioned default alias `Proposal\Future_Industrial_PLM_Meeting_Deck_EN.pptx`. Verified both files on disk at `33,094,128` bytes; V6 remained `2026-06-06 19:53:37`, and the default alias was refreshed immediately after. Treat `Future_Industrial_PLM_Meeting_Deck_EN.pptx` as the default working deck backed by V6 unless a newer explicit version is named.

Edit these decks with [[office-docs-com-automation]] (no Python/Node on this PC).
